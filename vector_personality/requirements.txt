# Vector Personality Project - Python Requirements

## Core SDK & Communication
# Local Anki Vector SDK (modified version in ../anki_vector/)
-e ../
aiogrpc>=1.4
cryptography
grpcio>=1.30.0

## Database
chromadb>=0.4.0         # Vector database for all persistence
# sqlalchemy>=1.4       # Optional ORM layer (not needed with ChromaDB)

## AI/ML & APIs
openai>=1.0.0           # ChatGPT API (GPT-4)
numpy>=1.19.0
torch>=1.9.0            # For YOLOv5
torchvision>=0.10.0     # For YOLOv5

## Audio & Speech
librosa>=0.9.0          # Audio processing
webrtcvad>=2.0.10      # Voice Activity Detection (VAD) - requires Visual C++ Build Tools on Windows
                       # Install via: pip install webrtcvad (if you have MSVC Build Tools installed)
                       # Or skip if VAD is not needed for your use case
PyAudio>=0.2.11         # Microphone input
pydub>=0.25.1           # Audio format conversion
scipy>=1.5.0            # Audio processing support

## Text-to-Speech
gTTS>=2.2.4             # Google Text-to-Speech
beautifulsoup4>=4.9     # gTTS dependency

## Utilities
python-dotenv>=0.19.0   # .env file support
requests>=2.25.0
Pillow>=8.0.0           # Image processing
dataclasses-json>=0.5   # JSON serialization

## YOLOv5 (Object Detection)
# YOLOv5 can be installed from GitHub or requirements:
# git+https://github.com/ultralytics/yolov5.git

## Development & Testing
pytest>=6.0.0           # Unit testing
pytest-asyncio>=0.18.0  # Async test support
black>=21.0             # Code formatting
flake8>=3.9.0          # Linting
mypy>=0.910             # Type checking

## Documentation
sphinx>=4.0.0           # Doc generation (optional)

## Notes on Versions
# - Python 3.9+ required (per project specification)
# - SQL Server Driver: ODBC Driver 17 for SQL Server (Windows installation)
# - YOLOv5: Can reuse existing models (yolov5n.pt, yolov5s.pt in workspace)
